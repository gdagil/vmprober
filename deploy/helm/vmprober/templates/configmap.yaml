apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vmprober.fullname" . }}
  labels:
    {{- include "vmprober.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
