apiVersion: v1
kind: Service
metadata:
  name: {{ include "vmprober.fullname" . }}
  labels:
    {{- include "vmprober.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "vmprober.selectorLabels" . | nindent 4 }}
